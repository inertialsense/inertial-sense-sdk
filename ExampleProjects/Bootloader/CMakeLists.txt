CMAKE_MINIMUM_REQUIRED(VERSION 2.8.13)

project(ISBootloaderExample)
SET(PROJECT_NAME "ISBootloaderExample")
SET(PROJECT_DIR "${CMAKE_CURRENT_LIST_DIR}")
SET(MAIN_CPP "ISBootloaderExample.cpp")

include(../../CMakeCommon.txt)

SET(MAIN_CPP "")
