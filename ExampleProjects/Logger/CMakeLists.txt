CMAKE_MINIMUM_REQUIRED(VERSION 2.8.13)

project(ISLoggerExample)
SET(PROJECT_NAME "ISLoggerExample")
SET(PROJECT_DIR "${CMAKE_CURRENT_LIST_DIR}")
SET(MAIN_CPP "ISLoggerExample.cpp")

include(../../CMakeCommon.txt)

SET(MAIN_CPP "")
